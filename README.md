# 🛠️ Air Technician Booking System (ระบบจองช่างแอร์)

นี่คือระบบจัดการการจองบริการช่างแอร์แบบ Full Stack ที่พัฒนาด้วย **MERN Stack** (MongoDB, Express.js, React, Node.js) โดยมีระบบ Authentication แยกตาม Role (Customer, Technician, Admin) และรองรับการเข้าสู่ระบบผ่าน Google OAuth 2.0

## ✨ คุณสมบัติหลัก (Features)

* **Authentication (JWT & Google OAuth):**
    * ระบบลงทะเบียนและเข้าสู่ระบบแยกตามบทบาท (Customer, Technician, Admin)
    * รองรับการเข้าสู่ระบบด้วย Google (Google Sign-In) เพื่อความสะดวก
* **Customer Features:**
    * ค้นหาช่างตามพื้นที่และบริการ
    * ดูรายละเอียดช่าง, เรตติ้ง, และรีวิว
    * ทำการจองบริการพร้อมระบุวันเวลาและสถานที่
    * ดูประวัติการจองและให้คะแนน/รีวิวช่างหลังเสร็จงาน
* **Technician Features:**
    * ลงทะเบียนพร้อมสถานะรอการอนุมัติจาก Admin
    * จัดการโปรไฟล์ส่วนตัวและบริการที่รับทำ
    * รับงาน/ยกเลิกงานที่ลูกค้าจอง
    * ดูประวัติงานที่ทำสำเร็จและรีวิวที่ได้รับ
* **Admin Features:**
    * จัดการผู้ใช้ (Customer) ทั้งหมด
    * อนุมัติ/ไม่อนุมัติบัญชีช่าง (Technician)
    * จัดการรายการบริการ (Service) และผู้ดูแลระบบ (Admin) อื่น ๆ
    * ดูภาพรวมสถิติการจองและรายได้

## ⚙️ เทคโนโลยีที่ใช้ (Tech Stack)

### Backend (Node.js/Express)
* **Database:** MongoDB / Mongoose
* **Framework:** Express.js
* **Authentication:** JWT (JSON Web Tokens), Passport.js, `passport-google-oauth20`
* **Hashing:** bcrypt
* **File Upload:** Multer & Cloudinary

### Frontend (React)
* **Framework:** React (สร้างด้วย Vite)
* **Styling:** Tailwind CSS
* **Routing:** `react-router-dom`
* **HTTP Client:** Axios
* **Notifications:** `react-hot-toast`

## 🚀 การติดตั้งและเรียกใช้งาน

คุณต้องติดตั้ง Dependencies ทั้งในส่วน Backend และ Frontend แยกกัน

### 1. การตั้งค่า Environment Variables

สร้างไฟล์ `.env` ในโฟลเดอร์ `backend` และเพิ่มตัวแปรดังต่อไปนี้:

```env
# ฐานข้อมูล MongoDB
MONGO_URI=your_mongodb_connection_string 

# JWT Secret (ใช้สำหรับเข้ารหัส/ถอดรหัส Token)
JWT_SECRET=your_jwt_secret_key

# Cloudinary (สำหรับจัดการรูปภาพ)
CLOUD_NAME=your_cloudinary_cloud_name
API_KEY=your_cloudinary_api_key
API_SECRET=your_cloudinary_api_secret

# Google OAuth 2.0 (สำหรับ Google Sign-In)
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
# ต้องตรงกับ Redirect URI ที่ลงทะเบียนใน Google Cloud Console
GOOGLE_CALLBACK_URL=http://localhost:5000/auth/google/callback
